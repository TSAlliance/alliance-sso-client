{
  "name": "@tsalliance/sso-nest",
  "version": "1.0.38",
  "description": "",
  "main": "dist/index",
  "scripts": {
    "build": "rimraf dist/ && tsc",
    "build:publish": "npm run build && npm version patch --no-git-tag-version && npm publish --access=public"
  },
  "dependencies": {
    "@nestjs/common": "^8.0.0",
    "@nestjs/core": "^8.0.0",
    "axios": "^0.24.0",
    "typeorm": "^0.2.41",
    "@nestjs/typeorm": "^8.0.2"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^8.0.0",
    "@nestjs/core": "^8.0.0",
    "@nestjs/typeorm": "^8.0.2"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "dist/**/*"
  ],
  "types": "dist/index.d.ts"
}
